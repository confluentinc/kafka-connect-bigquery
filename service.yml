name: kafka-connect-bigquery
lang: java
lang_version: "8"
git:
  enable: true
codeowners:
  enable: true
semaphore:
  enable: true
  pipeline_type: cp
  extra_deploy_args: "-Pjenkins"
  extra_build_args: "-Pjenkins"
  generate_connect_changelogs: true
  github_write_targets:
    - confluentinc/docs-kafka-connect-bigquery
